{
  "name": "Re-enable VGA Console",
  "description": "Re-enables VGA/graphics output for the Proxmox host console by removing the VGA disable kernel parameter. After running this template and rebooting, console output will be visible on the monitor again.",
  "execute_on": "proxmox",
  "commands": [
    {
      "script": "reenable-vga-console.sh"
    }
  ],
  "notes": "Run this template to restore console output to the VGA/graphics card. A reboot is required for changes to take effect."
}
