{
  "name": "001-set-properties",
  "execute_on": "proxmox",
  "description": "Sets default values for username, mountpoint, uid, and gid for the backup application.",
  "commands": [{ "script": "scripts/set-properties.sh"}],
  "outputs": ["username", "mountpoint", "uid", "gid"],
  "notes": "This template outputs the default values as outputs in JSON format."
}
