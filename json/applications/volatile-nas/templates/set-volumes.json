{
  "execute_on": "ve",
  "name": "Volumes Parameter",
  "description": "Set volume parameter for share only (after bind mount to avoid creation of default volume)",
  "commands": [
    {
      "properties": [
        { "id": "volumes", "value": "nas=/var/nas" }
      ]
    }
  ]
}
